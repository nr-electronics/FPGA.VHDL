transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vcom -93 -work work {C:/Users/nr.new/Desktop/VHDL_1_KEYs_LEDs/VHDL_1_KEYs_LEDs.vhd}

vcom -93 -work work {C:/Users/nr.new/Desktop/VHDL_1_KEYs_LEDs/simulation/modelsim/VHDL_1_KEYs_LEDs._test.vhd}

vsim -t 1ps -L altera -L lpm -L sgate -L altera_mf -L altera_lnsim -L maxii -L rtl_work -L work -voptargs="+acc"  1

add wave *
view structure
view signals
run -all
